I want to build a nix flake for this project. The flake should have settings such as "enable", "host" and "port", along with a "setting" section to declaratively define the app settings.

Once completed, test it (you are on a nixos machine), then write a detailed guide in ./docs on how to install this nix flake.

Then push and commit your changes.
